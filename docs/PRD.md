# LifeBoard - Product Requirements Document

> **원페이지 라이프 브리핑 서비스**
> Notion을 CMS로 활용하여, 내가 관심 있는 주제들의 최신 정보를 한 페이지에서 카드 형태로 브리핑해주는 개인화된 대시보드

---

## 1. 프로젝트 개요

### 1.1 목적

LifeBoard는 사용자가 관심 있는 주제(기술 트렌드, 건강, 재테크, 취미 등)의 최신 정보를 **매일 한 페이지에서 한눈에 확인**할 수 있는 라이프 브리핑 서비스이다. Notion 데이터베이스를 CMS로 활용하여, 비개발자도 콘텐츠를 쉽게 관리할 수 있도록 한다.

### 1.2 비전

"매일 아침, 나만의 브리핑 페이지를 열면 오늘 알아야 할 모든 것이 보인다."

### 1.3 핵심 가치

| 가치 | 설명 |
|------|------|
| **제로 클릭** | 페이지를 열면 클릭 없이 모든 정보가 한눈에 보인다 |
| **비주얼 브리핑** | 텍스트 나열이 아닌, 카드 기반 시각적 대시보드 |
| **비개발자 친화** | Notion에서 토픽 추가/수정만 하면 자동 반영 |
| **공유 가능** | 브리핑 카드를 이미지로 저장하거나 SNS에 공유 |

---

## 2. 타겟 사용자

### 2.1 페르소나

#### 페르소나 A: "정보 수집가" 지현 (32세, 스타트업 PM)
- **니즈**: 매일 기술 트렌드, 경쟁사 동향, 업계 뉴스를 빠르게 파악하고 싶다
- **현재 행동**: 뉴스레터 5개, RSS 피드, 트위터를 매일 30분씩 확인
- **기대**: 한 페이지에서 관심 분야 요약을 3분 안에 확인

#### 페르소나 B: "라이프 매니저" 수진 (28세, 직장인)
- **니즈**: 건강, 재테크, 자기계발 정보를 놓치지 않고 체크하고 싶다
- **현재 행동**: 여러 앱과 블로그를 산발적으로 확인
- **기대**: 출근 전 5분, 간결한 브리핑으로 하루를 시작

#### 페르소나 C: "지식 큐레이터" 민호 (35세, 블로거/크리에이터)
- **니즈**: 큐레이팅한 정보를 깔끔한 형태로 팔로워에게 공유하고 싶다
- **현재 행동**: Notion에 수동으로 정리 후 스크린샷 공유
- **기대**: 자동으로 예쁜 카드가 생성되어 바로 공유 가능

### 2.2 사용 시나리오

```
1. [편집] Notion에서 "AI 기술" 토픽을 추가하고, 오늘의 브리핑 내용을 작성
2. [확인] LifeBoard 페이지를 열면 "AI 기술" 카드가 자동으로 표시
3. [소비] 카테고리별로 정렬된 카드들을 스크롤하며 오늘의 브리핑 확인
4. [공유] 마음에 드는 브리핑 카드를 이미지로 저장하여 SNS에 공유
```

---

## 3. 핵심 기능 요구사항

### 3.1 Notion CMS 연동

| 항목 | 요구사항 |
|------|----------|
| **Topics DB 조회** | Notion API로 활성 토픽 목록을 조회하여 카테고리/아이콘/색상 정보 가져오기 |
| **Briefings DB 조회** | 오늘 날짜 기준 브리핑 데이터를 필터링하여 카드에 표시 |
| **캐싱** | ISR(Incremental Static Regeneration) 또는 Route Handler 캐시로 API 호출 최소화 |
| **에러 처리** | Notion API 실패 시 빈 상태(empty state) UI 표시 |

### 3.2 AI 기반 정보 수집/요약 (후순위)

> **Phase 4 이후 구현 예정** - MVP에서는 Notion에 수동 입력

| 항목 | 요구사항 |
|------|----------|
| **웹 검색** | 토픽 키워드 기반으로 최신 뉴스/정보 자동 수집 |
| **AI 요약** | 수집된 정보를 3~5줄로 요약하여 브리핑 생성 |
| **자동 갱신** | 매일 지정 시간에 자동으로 브리핑 생성 |

### 3.3 원페이지 브리핑 UI

| 항목 | 요구사항 |
|------|----------|
| **카드 기반 대시보드** | 각 토픽별 브리핑을 카드 형태로 표시 |
| **카테고리 그룹핑** | 같은 카테고리 토픽끼리 시각적으로 묶어 표시 |
| **날짜 네비게이션** | 오늘/어제/특정 날짜의 브리핑을 조회할 수 있는 날짜 선택 |
| **빈 상태** | 브리핑이 없는 토픽에 대한 빈 상태 UI |
| **로딩 상태** | 스켈레톤 UI로 데이터 로딩 중 표시 |

### 3.4 이미지 저장 및 SNS 공유

| 항목 | 요구사항 |
|------|----------|
| **카드 이미지 생성** | 개별 브리핑 카드를 PNG 이미지로 변환 |
| **전체 대시보드 캡처** | 전체 브리핑 페이지를 이미지로 저장 |
| **SNS 공유** | 트위터, 카카오톡 등 공유 링크 생성 |
| **OG 이미지** | 공유 시 미리보기 이미지 자동 생성 |

### 3.5 반응형 디자인

| 디바이스 | 레이아웃 |
|----------|----------|
| **데스크톱** (1024px+) | 3열 카드 그리드 |
| **태블릿** (768px~1023px) | 2열 카드 그리드 |
| **모바일** (< 768px) | 1열 카드 스택 |

---

## 4. 기술 아키텍처

### 4.1 시스템 구성도

```
┌─────────────┐     ┌──────────────────┐     ┌──────────────┐
│   Notion     │────▶│   Next.js App    │────▶│   사용자      │
│ (CMS/DB)     │     │  (App Router)    │     │  (브라우저)   │
└─────────────┘     └──────────────────┘     └──────────────┘
                            │
                    ┌───────┴───────┐
                    │               │
              ┌─────▼─────┐  ┌─────▼──────┐
              │  Route     │  │  Server    │
              │  Handlers  │  │  Components│
              │  (API)     │  │  (UI)      │
              └───────────┘  └────────────┘
```

### 4.2 데이터 플로우

```
1. [Server Component] 페이지 로드 시 Notion API 호출
2. [lib/notion.ts]    Topics DB → 토픽 목록 조회 (카테고리, 아이콘, 색상)
3. [lib/notion.ts]    Briefings DB → 오늘 날짜 브리핑 조회 (토픽별 내용)
4. [Server Component] 데이터를 브리핑 카드 컴포넌트에 전달
5. [Client Component] 날짜 변경, 공유 등 인터랙션 처리
```

### 4.3 환경변수

```env
# Notion API
NOTION_API_KEY=secret_xxx          # Notion Integration 토큰
NOTION_TOPICS_DB_ID=xxx            # Topics 데이터베이스 ID
NOTION_BRIEFINGS_DB_ID=xxx         # Briefings 데이터베이스 ID

# 사이트
NEXT_PUBLIC_SITE_URL=https://lifeboard.example.com
```

### 4.4 기술 스택 (기존 유지 + 확장)

| 영역 | 기술 | 비고 |
|------|------|------|
| 프레임워크 | Next.js 16.1.6 (App Router) | 기존 유지 |
| UI | React 19.2.3 | 기존 유지 |
| 언어 | TypeScript 5 | 기존 유지 |
| 스타일링 | Tailwind CSS 4 | 기존 유지 |
| 컴포넌트 | Shadcn/ui (Card, Badge, Tabs 등) | 기존 유지 + 확장 |
| 다크모드 | next-themes | 기존 유지 |
| Notion API | @notionhq/client | **신규 추가** |
| 이미지 생성 | html-to-image | **신규 추가** (Phase 3) |
| 날짜 처리 | date-fns | **신규 추가** |
| 유틸리티 | cn() (clsx + tailwind-merge) | 기존 유지 |

---

## 5. Notion 데이터베이스 설계

### 5.1 Topics DB (토픽 관리)

사용자가 관심 있는 주제를 관리하는 데이터베이스

| 속성명 | Notion 타입 | 설명 | 예시 |
|--------|-------------|------|------|
| `Name` | Title | 토픽 이름 | "AI 기술 동향" |
| `Category` | Select | 카테고리 분류 | "기술", "건강", "재테크", "라이프" |
| `Icon` | Rich Text | 이모지 또는 아이콘 코드 | "🤖" |
| `Color` | Select | 카테고리 색상 | "blue", "green", "orange", "purple" |
| `Active` | Checkbox | 활성화 여부 | true/false |
| `Order` | Number | 표시 순서 | 1, 2, 3... |
| `Description` | Rich Text | 토픽 설명 | "인공지능 관련 최신 기술 트렌드" |

### 5.2 Briefings DB (브리핑 데이터)

각 토픽에 대한 일별 브리핑 콘텐츠

| 속성명 | Notion 타입 | 설명 | 예시 |
|--------|-------------|------|------|
| `Title` | Title | 브리핑 제목 | "OpenAI, GPT-5 발표 임박" |
| `Topic` | Relation | Topics DB 연결 | → "AI 기술 동향" |
| `Date` | Date | 브리핑 날짜 | 2026-02-10 |
| `Summary` | Rich Text | 요약 내용 (3~5줄) | "OpenAI가 다음 주..." |
| `Source` | URL | 출처 링크 | "https://..." |
| `SourceName` | Rich Text | 출처명 | "TechCrunch" |
| `Importance` | Select | 중요도 | "high", "medium", "low" |
| `Tags` | Multi-select | 태그 | "AI", "GPT", "LLM" |

### 5.3 Notion API 코드 패턴

```typescript
// lib/notion.ts

import { Client } from "@notionhq/client"

const notion = new Client({
  auth: process.env.NOTION_API_KEY,
})

// 활성 토픽 조회
export async function getActiveTopics(): Promise<Topic[]> {
  const response = await notion.databases.query({
    database_id: process.env.NOTION_TOPICS_DB_ID!,
    filter: {
      property: "Active",
      checkbox: { equals: true },
    },
    sorts: [
      { property: "Order", direction: "ascending" },
    ],
  })
  return response.results.map(parseTopicPage)
}

// 특정 날짜 브리핑 조회
export async function getBriefings(date: string): Promise<Briefing[]> {
  const response = await notion.databases.query({
    database_id: process.env.NOTION_BRIEFINGS_DB_ID!,
    filter: {
      property: "Date",
      date: { equals: date },
    },
  })
  return response.results.map(parseBriefingPage)
}
```

---

## 6. UI/UX 요구사항

### 6.1 페이지 구조

기존 랜딩 페이지(about, blog, pricing, contact, login)를 모두 제거하고, 브리핑 대시보드 중심의 단일 페이지 앱으로 전환한다.

```
/ (메인)
├── 헤더: 로고 + 날짜 선택 + 테마 토글
├── 브리핑 대시보드
│   ├── 날짜 표시 ("2026년 2월 10일 월요일")
│   ├── 카테고리 필터 탭 (전체 / 기술 / 건강 / 재테크 / 라이프)
│   └── 브리핑 카드 그리드
│       ├── 카드 1: [아이콘] 토픽명 / 요약 / 출처 / 태그
│       ├── 카드 2: ...
│       └── 카드 N: ...
└── 푸터: 간소화 (저작권 + 테마)
```

### 6.2 브리핑 카드 와이어프레임

```
┌──────────────────────────────────────┐
│ 🤖 AI 기술 동향            ★ high   │  ← 헤더: 아이콘 + 토픽명 + 중요도
│──────────────────────────────────────│
│                                      │
│ OpenAI, GPT-5 발표 임박              │  ← 제목 (bold)
│                                      │
│ OpenAI가 다음 주 GPT-5를 공식        │  ← 요약 (3~5줄)
│ 발표할 예정이다. 이번 모델은         │
│ 멀티모달 성능이 크게 향상되었으며...  │
│                                      │
│ 📎 TechCrunch                        │  ← 출처
│                                      │
│ [AI] [GPT] [LLM]           [공유]   │  ← 태그 + 공유 버튼
└──────────────────────────────────────┘
```

### 6.3 카테고리별 색상 시스템

```css
/* globals.css에 추가할 카테고리 색상 변수 */
:root {
  --category-tech: 221 83% 53%;     /* 파랑 */
  --category-health: 142 71% 45%;   /* 초록 */
  --category-finance: 38 92% 50%;   /* 주황 */
  --category-life: 262 83% 58%;     /* 보라 */
}
```

### 6.4 상태별 UI

| 상태 | UI |
|------|------|
| **로딩** | 카드 스켈레톤 (3개) + 펄스 애니메이션 |
| **빈 상태** | "오늘의 브리핑이 아직 없습니다" + 일러스트 |
| **에러** | "정보를 불러올 수 없습니다. 잠시 후 다시 시도해주세요" + 재시도 버튼 |
| **정상** | 카드 그리드 표시 |

---

## 7. API 설계

### 7.1 Route Handlers

```
app/
├── api/
│   ├── topics/
│   │   └── route.ts          GET /api/topics - 토픽 목록 조회
│   ├── briefings/
│   │   └── route.ts          GET /api/briefings?date=2026-02-10 - 브리핑 조회
│   └── share/
│       └── route.ts          POST /api/share - 공유 이미지 생성 (Phase 3)
```

### 7.2 타입 정의

```typescript
// lib/types.ts

// 카테고리 타입
export type Category = "tech" | "health" | "finance" | "life"

// 중요도 타입
export type Importance = "high" | "medium" | "low"

// 토픽
export interface Topic {
  id: string
  name: string
  category: Category
  icon: string
  color: string
  active: boolean
  order: number
  description: string
}

// 브리핑
export interface Briefing {
  id: string
  title: string
  topicId: string
  date: string           // "YYYY-MM-DD"
  summary: string
  source: string | null  // URL
  sourceName: string | null
  importance: Importance
  tags: string[]
}

// 토픽 + 브리핑 결합 (UI용)
export interface TopicWithBriefings {
  topic: Topic
  briefings: Briefing[]
}
```

### 7.3 lib 모듈 구조

```
lib/
├── config.ts        # 사이트 설정 (LifeBoard용으로 전면 변경)
├── utils.ts         # cn() 유틸리티 (기존 유지)
├── types.ts         # 타입 정의 (신규)
├── notion.ts        # Notion API 클라이언트 (신규)
└── date.ts          # 날짜 유틸리티 (신규)
```

---

## 8. 컴포넌트 구조

### 8.1 신규 컴포넌트

```
components/
├── ui/                       # Shadcn/ui (기존 유지)
│   ├── card.tsx              # 재활용
│   ├── badge.tsx             # 재활용
│   ├── tabs.tsx              # 재활용
│   ├── button.tsx            # 재활용
│   └── skeleton.tsx          # 신규 추가 (npx shadcn@latest add skeleton)
│
├── briefing/                 # 브리핑 전용 컴포넌트 (신규)
│   ├── briefing-card.tsx     # 브리핑 카드 (Server Component)
│   ├── briefing-grid.tsx     # 카드 그리드 레이아웃
│   ├── briefing-skeleton.tsx # 로딩 스켈레톤
│   ├── category-filter.tsx   # 카테고리 필터 탭 (Client Component)
│   ├── date-navigator.tsx    # 날짜 네비게이터 (Client Component)
│   ├── empty-state.tsx       # 빈 상태 UI
│   └── share-button.tsx      # 공유 버튼 (Client Component, Phase 3)
│
├── header.tsx                # 간소화 (로고 + 날짜 + 테마 토글)
├── footer.tsx                # 간소화 (저작권만)
├── theme-provider.tsx        # 기존 유지
└── theme-toggle.tsx          # 기존 유지
```

### 8.2 Server/Client 컴포넌트 분리 원칙

| 컴포넌트 | 타입 | 이유 |
|----------|------|------|
| `briefing-card.tsx` | Server | Notion 데이터 표시만, 인터랙션 없음 |
| `briefing-grid.tsx` | Server | 카드 레이아웃만 |
| `category-filter.tsx` | Client | 탭 클릭 인터랙션 (useState) |
| `date-navigator.tsx` | Client | 날짜 변경 인터랙션 (useState) |
| `share-button.tsx` | Client | 브라우저 API (클립보드, 다운로드) |
| `page.tsx` | Server | 데이터 페칭 (async) |

---

## 9. 개발 마일스톤

### Phase 1: 기반 정리 (Week 1)
- [x] PRD 작성
- [ ] 기존 랜딩 페이지 제거 (about, blog, pricing, contact, login 페이지/컴포넌트)
- [ ] `lib/config.ts` LifeBoard용으로 전면 재작성
- [ ] Header/Footer 간소화
- [ ] `lib/types.ts` 타입 정의
- [ ] 빈 대시보드 레이아웃 구성

### Phase 2: Notion 연동 + 핵심 UI (Week 2~3)
- [ ] `@notionhq/client`, `date-fns` 패키지 설치
- [ ] Notion 데이터베이스 생성 (Topics DB, Briefings DB)
- [ ] `lib/notion.ts` 구현 (토픽 조회, 브리핑 조회)
- [ ] `lib/date.ts` 날짜 유틸리티 구현
- [ ] Route Handlers 구현 (`/api/topics`, `/api/briefings`)
- [ ] 브리핑 카드 컴포넌트 (`briefing-card.tsx`)
- [ ] 카드 그리드 레이아웃 (`briefing-grid.tsx`)
- [ ] 카테고리 필터 (`category-filter.tsx`)
- [ ] 날짜 네비게이터 (`date-navigator.tsx`)
- [ ] 스켈레톤 로딩 UI
- [ ] 빈 상태 / 에러 상태 UI
- [ ] 카테고리별 색상 시스템 (globals.css)

### Phase 3: 공유 기능 (Week 4)
- [ ] `html-to-image` 패키지 설치
- [ ] 카드 이미지 생성 기능
- [ ] SNS 공유 (Web Share API)
- [ ] OG 이미지 동적 생성 (`app/api/og/route.ts`)

### Phase 4: AI 정보 수집/요약 (후순위)
- [ ] AI 요약 엔진 선정 및 연동
- [ ] 웹 검색 API 연동
- [ ] 자동 브리핑 생성 파이프라인
- [ ] Cron Job 설정 (매일 자동 갱신)

### Phase 5: 고도화 (후순위)
- [ ] 사용자 인증 (개인 Notion DB 연결)
- [ ] 토픽 구독/관리 UI
- [ ] 브리핑 히스토리 아카이브
- [ ] PWA 지원
- [ ] 알림 기능 (새 브리핑 도착 시)

---

## 10. 성공 지표

### 10.1 기술 KPI

| 지표 | 목표 |
|------|------|
| **Lighthouse 성능 점수** | 90+ |
| **LCP (Largest Contentful Paint)** | < 2.5초 |
| **FID (First Input Delay)** | < 100ms |
| **CLS (Cumulative Layout Shift)** | < 0.1 |
| **빌드 성공률** | 100% (TypeScript strict) |
| **Notion API 응답 시간** | < 1초 (캐시 적용) |

### 10.2 사용자 KPI

| 지표 | 목표 |
|------|------|
| **브리핑 확인 시간** | < 3분 (전체 카드 스캔) |
| **일일 방문** | 매일 1회 이상 |
| **공유 카드 생성** | 주 2회 이상 |
| **토픽 추가/수정 용이성** | Notion에서 5분 내 반영 |

---

## 11. 부록

### 11.1 신규 패키지

| 패키지 | 용도 | 설치 Phase |
|--------|------|-----------|
| `@notionhq/client` | Notion API 공식 클라이언트 | Phase 2 |
| `date-fns` | 날짜 포맷팅/계산 유틸리티 | Phase 2 |
| `html-to-image` | DOM → 이미지 변환 | Phase 3 |

Shadcn/ui 추가 컴포넌트:

```bash
npx shadcn@latest add skeleton    # Phase 2
npx shadcn@latest add tabs        # 이미 설치됨
npx shadcn@latest add badge       # 이미 설치됨
```

### 11.2 신규 파일 목록

```
docs/
└── PRD.md                        # 본 문서

lib/
├── types.ts                      # 타입 정의
├── notion.ts                     # Notion API 클라이언트
└── date.ts                       # 날짜 유틸리티

app/
└── api/
    ├── topics/route.ts           # 토픽 API
    ├── briefings/route.ts        # 브리핑 API
    └── share/route.ts            # 공유 API (Phase 3)

components/
└── briefing/
    ├── briefing-card.tsx         # 브리핑 카드
    ├── briefing-grid.tsx         # 카드 그리드
    ├── briefing-skeleton.tsx     # 로딩 스켈레톤
    ├── category-filter.tsx       # 카테고리 필터
    ├── date-navigator.tsx        # 날짜 네비게이터
    ├── empty-state.tsx           # 빈 상태 UI
    └── share-button.tsx          # 공유 버튼 (Phase 3)
```

### 11.3 삭제 대상 (Phase 1)

**페이지:**
- `app/about/page.tsx`
- `app/blog/page.tsx`
- `app/blog/[slug]/page.tsx`
- `app/pricing/page.tsx`
- `app/contact/page.tsx`
- `app/login/page.tsx`

**컴포넌트:**
- `components/contact-form.tsx`
- `components/login-form.tsx`
- `components/pricing-cards.tsx`
- `components/mobile-menu.tsx` (Header 간소화 시 불필요할 수 있음)

### 11.4 config.ts 변경 방향

기존 `lib/config.ts`의 랜딩 페이지용 데이터(features, about, pricing, blog, contact 등)를 모두 제거하고, LifeBoard 전용 설정으로 전면 교체한다.

```typescript
// lib/config.ts 변경 후 구조 (예시)
export const config = {
  name: "LifeBoard",
  description: "나만의 라이프 브리핑 대시보드",
  url: process.env.NEXT_PUBLIC_SITE_URL || "https://lifeboard.example.com",

  // 카테고리 정의
  categories: [
    { id: "tech", label: "기술", color: "blue", icon: "💻" },
    { id: "health", label: "건강", color: "green", icon: "🏃" },
    { id: "finance", label: "재테크", color: "orange", icon: "💰" },
    { id: "life", label: "라이프", color: "purple", icon: "🌱" },
  ],

  // 네비게이션 (간소화)
  nav: [
    { title: "브리핑", href: "/" },
  ],

  // 푸터 (간소화)
  footer: {
    description: "Notion 기반 라이프 브리핑 대시보드",
  },
} as const
```

### 11.5 기존 패턴 재활용

| 패턴 | 파일 | 활용 방식 |
|------|------|----------|
| `cn()` 유틸리티 | `lib/utils.ts` | 조건부 클래스 병합에 그대로 사용 |
| Shadcn/ui Card | `components/ui/card.tsx` | 브리핑 카드의 기반 컴포넌트로 활용 |
| Shadcn/ui Badge | `components/ui/badge.tsx` | 태그, 중요도 표시에 활용 |
| Shadcn/ui Tabs | `components/ui/tabs.tsx` | 카테고리 필터에 활용 |
| ThemeProvider | `components/theme-provider.tsx` | 다크모드 그대로 유지 |
| theme-toggle | `components/theme-toggle.tsx` | 헤더에 유지 |
| Server Component 기본 | 전체 | 인터랙션 필요 시에만 "use client" |
